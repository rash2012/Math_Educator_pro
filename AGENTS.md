# تعليمات وقواعد الوكلاء (AGENTS.md)
# Multi-Layer AI Agent System Guidelines for Math Educator Pro

هذا الملف يحدد القواعد الثابتة، المعايير البصرية، والميثاق الحاكم لعمل الوكيل الذكي (AI Assistant / Coding Agent) ونظام الوكلاء المتعدد الطبقات (Multi-Layer Agent System) ضمن مشروع **Math Educator Pro**.

---

## 🔒 الميثاق الأمني وقواعد التعامل مع الملفات (Critical Safety Rules)

1. **حظر الحذف التام (No Deletion Without Approval):**
   - **يُمنع منعاً باتاً** على الوكيل حذف أي ملف أو مجلد من المشروع بدون موافقة صريحة ومسبقة من المستخدم.
   - إذا اقتضت الضرورة حذف ملف أو استبداله، يجب تقديم طلب صريح موضحاً فيه السبب وانتظار تأكيد المستخدم.

2. **الشفافية التامة وتفصيل التعديلات (Modification Reporting):**
   - في نهاية كل مهمة أو رد، يلتزم الوكيل بتقديم **تقرير واضح وموجز** يبيّن بدقة كافة التعديلات، الإضافات، والتحسينات التي طرأت على الكود والمكونات.

---

## 🏛️ معمارية نظام الوكلاء متعدد الطبقات (4-Layer Agent Architecture)

تم تنظيم قدرات الذكاء الاصطناعي والتطوير البرمجي في التطبيق وفق 4 طبقات متكاملة:

```
┌─────────────────────────────────────────────────────────────────────────────┐
│ 1. طبقة التنسيق وتوحيد الهوية البصرية (Orchestrator & Visual Harmonizer)      │
│    • إدارة التدفق الشامل وتوجيه الطلبات.                                   │
│    • توحيد الهوية البصرية (Visual Identity) والألوان والخطوط والمسافات.      │
│    • توحيد واجهات الأقسام (Sections Consistency) والبطاقات والأزرار.        │
└──────────────────────────────────────┬──────────────────────────────────────┘
                                       ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│ 2. طبقة المنهاج والمعايير البيداغوجية (Curriculum & Pedagogical Agent)       │
│    • ضبط المصطلحات والرموز وفق المنهاج السوري للرياضيات (الثالث الثانوي).   │
│    • تصنيف المفاهيم (مبرهنات، تعاريف، نتائج، شروط انطلاق، خواص جبرية).    │
│    • معايير كتابة الأسئلة الامتحانية وفق سلم التصحيح المعتمد.                │
└──────────────────────────────────────┬──────────────────────────────────────┘
                                       ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│ 3. طبقة التوليد التخصصية (Specialized Generation Engines)                   │
│    • محرك شرح وتلخيص الدروس والمراجعة الشاملة للوحدة.                        │
│    • محرك بناء التمارين وبنوك الأسئلة والحلول النموذجية.                     │
│    • محرك المشتتات وتشخيص الأخطاء المفاهيمية (Misconceptions Generator).    │
│    • محرك صياغة الرموز والمعادلات بصيغة LaTeX القياسية.                      │
└──────────────────────────────────────┬──────────────────────────────────────┘
                                       ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│ 4. طبقة التدقيق والنقد الذاتي (Critic, Validator & LaTeX Verifier)          │
│    • الفحص الحسابي والمنطقي للنتائج وصحة الإجابة المعتمدة بنسبة 100%.       │
│    • التدقيق الآلي لسلامة كود LaTeX وقاعدة الدولار المفرد $...$ فقط.         │
│    • التحقق من سلامة كائنات JSON وخلوها من التكرار أو الحقول الناقصة.        │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 🎨 الميثاق البصري الموحد لتطبيق Math Educator Pro (Visual Identity Tokens)

يجب على طبقة التنسيق (Visual Harmonizer) إلزام كافة المكونات بالمعايير التالية:

### 1. نظام الألوان (Color Palette):
- **اللون الأساسي (Primary):** درجات البنفسجي والأزرق النيلي الملكي (`violet-600` / `indigo-600` / `indigo-700`) للعناوين الرئيسية، الأزرار البارزة، والتبويبات النشطة.
- **لون النجاح والصواب (Success):** درجات الزمردي (`emerald-600` / `emerald-700`) للإجابات الصحيحة، مؤشرات الإنجاز، والنتائج النموذجية.
- **لون التحذيرات والمطبات (Warning/Alert):** درجات العنبري (`amber-500` / `amber-600`) ودرجات الوردي/الأحمر (`rose-500` / `rose-600`) للمطبات الامتحانية والأخطاء الشائعة.
- **الخلفيات والسطوح (Surfaces):** خلفيات بيضاء نظيفة أو رمادية خفيفة دافئة (`bg-white` / `bg-slate-50/70`)، مع تباين عالي يضمن راحة العين وسهولة القراءة أثناء المذاكرة.

### 2. التناسق الهيكلي والبطاقات (Containers & Radii):
- **انحناء الحواف الموحد:** استخدام `rounded-2xl` للبطاقات والصناديق الرئيسية، و`rounded-xl` للأزرار وحقول الإدخال، و`rounded-lg` للبادجات والشرائح.
- **الظلال الناعمة:** استخدام الظلال الهادئة (`shadow-xs` / `shadow-sm`) وتجنب الظلال الحادة أو التدرجات المبالغ فيها.
- **المسافات المنتظمة:** الحفاظ على هوامش داخلية وافرة لا تقل عن `p-4` إلى `p-6` داخل البطاقات.

### 3. معايير النصوص والرياضيات والمنهاج السوري (Typography, LaTeX & Curriculum Standards):
- **الخطوط:** الخطوط العربية الحديثة والواضحة (`Tajawal`, `Cairo`, `Amiri` للرياضيات).
- **قاعدة الدولار المفرد الصارمة ($ ... $):** كتابة كافة الرموز والمعادلات الرياضية بصيغة LaTeX داخل علامة الدولار المفردة `$...$` حصراً (يُمنع منعاً باتاً استخدام `$$...$$` لأنها تكسر محاذاة النص العربي وتشوّه اتجاه RTL وتوافق KaTeX).
- **قواعد LaTeX الرياضية الصارمة (Strict Mathematical LaTeX Protocol):**
  1. **LaTeX ككود برمجي لا يُمس (LaTeX Code Inviolability):**
     - يُعرّف كود LaTeX ككود برمجي بنيوي دقيق ومقدس لا يُمس.
     - **يُمنع منعاً باتاً حذف أو إسقاط أي شرطة مائلة `\`** من أي أمر LaTeX؛ ويُلزم كتابة الأوامر كاملة وبأسمائها البرمجية الصحيحة دائماً (مثل: `\frac`, `\sqrt`, `\vec`, `\overrightarrow`, `\ln`, `\exp`, `\lim`, `\infty`, `\cdot`, `\Delta`, `\pi`, `\alpha`, `\beta`, `\theta`, إلخ).
  2. **الاستخدام السليم والمتطابق لـ `\left` و `\right`:**
     - استخدام `\left` و `\right` بشكل متطابق وسليم لكافة الأقواس الكبيرة والحواصر والمحددات لضبط قياس الأقواس تلقائياً (`\left( ... \right)`, `\left[ ... \right]`, `\left| ... \right|`, `\left\{ ... \right\}`). يُمنع ترك `\left` دون `\right` متوافق.
  3. **قواعد صياغة الأشعة والمتجهات الصارمة (Vector Protocols):**
     - **للشعاع بحرف واحد:** استخدم فقط `\vec{u}` (أو `\vec{v}`, `\vec{w}`, `\vec{n}`, `\vec{i}`, `\vec{j}`, `\vec{k}`)؛ يُحظر استخدام `\overrightarrow` لحرف واحد.
     - **للشعاع بين نقطتين (حرفين):** استخدم فقط overrightarrow مثل `\overrightarrow{AB}` (أو `\overrightarrow{CD}`, `\overrightarrow{AC}`, `\overrightarrow{BC}`)؛ يُحظر استخدام `\vec` لحرفين.
  4. **الفحص والتدقيق الإلزامي قبل الإخراج (Equation Inspection):**
     - فحص كل معادلة وصيغة رياضية والتأكد من صحتها الحسابية وتطابق أقواسها ورموزها وسلامة علامات الدولار المفردة قبل الإخراج أو التوليد.
- **التوافق الصارم مع المنهاج السوري (الثالث الثانوي العلمي):**
  - **حظر الجداء الخارجي:** الجداء الشعاعي الخارجي ($\vec{u} \times \vec{v}$) **غير موجود إطلاقاً** في المنهاج السوري؛ المعتمد حصراً هو **الجداء السلمي** $\vec{u} \cdot \vec{v}$ وشروطه التحليلية والهندسية.
  - **حظر رمز $\sum$ للمجاميع:** لا يُستخدم رمز $\sum$ في المنهاج السوري للمتتاليات أو المجاميع المحدودة، وتُكتب المجاميع دوماً بنقاط التتابع $\cdots$ (مثال: $S_n = u_0 + u_1 + \cdots + u_n$).
  - **المجالات:** كتابة المجالات المفتوحة بالصيغة السورية $]a, b[$ والمغلقة $[a, b]$.
  - **المصطلحات المعتمدة:** استخدام مصطلح "تابع" (وتوابعه) حصراً ومنع استخدام كلمة "دالة".
  - **التدقيق العلمي 100%:** مراجعة وتدقيق حلول التمارين والتطبيقات وتدريبات الدروس والاختبارات بنسبة 100% مع خطوات حسابية مبررة بدقة وسلم تصحيح رسمي.

---

## 📐 إرشادات الأقسام ومطابقة الأداء

- **قسم شرح الدروس (Lesson View / Summaries):** الحفاظ على تسلسل (الأفكار النظرية ➔ أمثلة تطبيقية ➔ مراجعة شاملة للوحدة ➔ اختبار الوحدة ➔ الخريطة الذهنية).
- **ميدان التدريب بالأنماط (Pattern-Guided Trainer):** الحفاظ على عرض الخيارات الأربعة مع تشخيص المشتت، وتوفير خيارات العرض الواسع والمدمج.
- **دعم الطباعة (Print & Export Support):** ضمان تطبيق فواصل الصفحات النظيفة (`page-break-before`, `no-print`) لإخراج ملفات PDF قابلة للطباعة المدرسية بجودة عالية.
